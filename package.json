{
  "name": "filigrane",
  "version": "0.0.1",
  "description": "Accelérer l'affichage environnemental de la filière textile française",
  "author": "MTES <contact@beta.gouv.fr>",
  "license": "MIT",
  "private": true,
  "scripts": {
    "build": "rimraf dist && npm run build:init && parcel build index.html --public-url ./",
    "build:init": "mkdir -p dist && cp -r public/* dist/",
    "deploy": "npm run build && gh-pages --dist dist/",
    "start:dev": "npm run build:init && parcel serve index.html --no-cache",
    "start": "PARCEL_ELM_NO_DEBUG=1 npm run start:dev",
    "test": "elm-test"
  },
  "devDependencies": {
    "@parcel/transformer-elm": "^2.0.0-rc.0",
    "@parcel/transformer-sass": "^2.0.0-rc.0",
    "bootstrap": "^5.1.1",
    "elm": "^0.19.1-5",
    "elm-analyse": "^0.16.5",
    "elm-format": "^0.8.5",
    "elm-test": "^0.19.1-revision7",
    "gh-pages": "^3.2.3",
    "parcel": "^2.0.0-rc.0",
    "rimraf": "^3.0.2"
  }
}
