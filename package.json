{
  "name": "elm-kitten-sample-app",
  "version": "0.0.1",
  "description": "",
  "author": "You",
  "license": "MIT",
  "scripts": {
    "build": "parcel build public/index.html && cp -r public/img dist/ && cp -r public/markdown dist/",
    "deploy": "npm run build && gh-pages --dist dist/",
    "start:dev": "parcel serve public/index.html --port 3000 --no-cache",
    "start": "PARCEL_ELM_NO_DEBUG=1 npm run start:dev",
    "test": "elm-test"
  },
  "devDependencies": {
    "@parcel/transformer-elm": "^2.0.0-rc.0",
    "@parcel/transformer-sass": "^2.0.0-rc.0",
    "bootstrap": "^5.1.1",
    "elm": "^0.19.1-5",
    "elm-analyse": "^0.16.5",
    "elm-format": "^0.8.5",
    "elm-test": "^0.19.1-revision7",
    "gh-pages": "^3.2.3"
  },
  "dependencies": {
    "parcel": "^2.0.0-rc.0"
  }
}
